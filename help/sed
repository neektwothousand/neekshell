Usage
  !sed <regex> (on reply to a text message)

Description
  sends a message transformed and filtered based on <regex>

Example (basic usage)
  !sed s/hello/hi/ (transforms the first "hello" to "hi")
  !sed s/hello/hi/g (transforms every "hello" to "hi")
